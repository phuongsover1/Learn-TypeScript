"use strict";
let myTuple;
// myTuple = [1, 'phuong', 'df']; the tuple has fixed-size
// myTuple = ['phuong', 1]; // the type order is wrong
myTuple = [1, 'phuong'];
let numbers = [1, '2', 3];
numbers.push(2);
numbers.push(3);
console.log(numbers);
numbers.pop();
numbers.pop();
numbers.pop();
numbers.pop();
numbers.pop();
numbers.push('dfs');
console.log(numbers);
